<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_library_loan_list" model="ir.ui.view">
        <field name="name">library.loan.list</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <tree string="Prestiti"
                  decoration-danger="is_overdue == True"
                  decoration-muted="state == 'returned'">
                <field name="book_id"/>
                <field name="borrower_id"/>
                <field name="loan_date"/>
                <field name="return_date"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'returned'"
                       decoration-info="state == 'active'"/>
                <field name="is_overdue" column_invisible="True"/>
                <button name="action_return" type="object"
                        string="Restituisci" class="btn-secondary"
                        invisible="state == 'returned'"/>
            </tree>
        </field>
    </record>

    <record id="view_library_loan_form" model="ir.ui.view">
        <field name="name">library.loan.form</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <form string="Prestito">
                <header>
                    <button name="action_return" type="object"
                            string="Segna come Restituito"
                            class="btn-primary"
                            invisible="state == 'returned'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="active,returned"/>
                </header>
                <sheet>
                    <group>
                        <group string="Dettagli Prestito">
                            <field name="book_id"/>
                            <field name="borrower_id"/>
                        </group>
                        <group string="Date">
                            <field name="loan_date"/>
                            <field name="return_date"/>
                            <field name="is_overdue" widget="boolean_toggle" readonly="1"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_library_loan" model="ir.actions.act_window">
        <field name="name">Prestiti</field>
        <field name="res_model">library.loan</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>
